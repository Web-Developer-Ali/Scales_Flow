import { AssignTeamClient } from "./assign-team-client";

export const metadata = {
  title: "Assign Team - CRM Admin",
  description: "Assign sales reps to managers",
};

export default function AssignTeamPage() {
  return <AssignTeamClient />;
}
